# ADR-005 — Feature Store: Local Parquet Without a Datalake

**Status:** Accepted  
**Date:** 2026-05-02

## Context

The target production environment uses Databricks Feature Store backed by Azure Data Lake Storage (ADLS) or S3 — a persistent, distributed, cloud-managed storage layer. The feature store is populated after each training cycle and survives machine restarts, team changes, and environment recreations.

In this project, the feature store is a local Parquet file at `data/processed/feature_store.parquet`, generated by `src/features/feature_store.py`. This file is gitignored and not tracked by any remote storage. It is recreated from scratch by running `make train` against the static `creditcard.csv` dataset.

This means:
- There is no persistent datalake backing the feature store
- The file does not survive a machine wipe or a fresh clone without re-running training
- DVC only tracks the raw CSV (`data/raw/creditcard.csv`), not the processed features

## Decision

We deliberately accept local Parquet without a remote datalake for the following reasons:

1. **Static dataset**: The source data (`creditcard.csv`) never changes. Re-running `make train` always produces a byte-identical `feature_store.parquet`. Persistence adds no value when the output is deterministic.

2. **Incremental upsert pattern is preserved**: The `upsert_features()` function implements the correct production pattern — incremental merge by `transaction_id`, never full-flush. The pattern is what the datathon evaluates, not the storage backend.

3. **Scope**: Configuring a DVC remote (S3, GCS, Google Drive) would add infrastructure overhead without changing the evaluated behavior. The datathon does not require a live remote storage.

4. **Databricks equivalence**: Databricks Feature Store persists data as Delta files, which are Parquet files with a transaction log on top. The data model is identical — only the infrastructure layer differs. In production, `STORE_PATH` would point to an ADLS or S3 path instead of a local filesystem path.

## Consequences

- Any contributor must run `make train` after cloning to materialize the feature store locally.
- The CLAUDE.md documents this as a required setup step.
- If the dataset were to receive new transactions in a real deployment, the correct next step would be: configure a DVC remote (S3/ADLS), track `feature_store.parquet` via `dvc add`, and push via `dvc push` after each training cycle.
- This gap is acknowledged and can be explained to the banca as a deliberate scope decision, not an oversight.

## Alternatives Considered

| Alternative | Reason Rejected |
|---|---|
| DVC remote (S3/Google Drive) | Adds infra config with no measurable benefit for a static dataset |
| SQLite as feature store | More complex than Parquet, no benefit for batch-only workloads |
| Databricks / Delta Lake | Out of scope for a local development datathon environment |
